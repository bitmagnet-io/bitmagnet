#import "../fragments/TorrentContentSearchResult"

query Health {
  health {
    status
    checks {
      key
      status
      timestamp
      error
    }
  }
  workers {
    all {
      key
      started
    }
  }
}
