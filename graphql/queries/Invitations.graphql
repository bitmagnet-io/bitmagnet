#import "../fragments/Invitation"

query Invitations($input: ListInvitationsInput) {
  auth {
    listInvitations(input: $input) {
      invitations {
        ...Invitation
      }
      totalCount
    }
  }
}
