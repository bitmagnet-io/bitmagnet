#import "../fragments/TorrentContentResult"

query SearchTorrentContent(
  $query: SearchQueryInput
  $facets: TorrentContentFacetsInput
) {
  search {
    torrentContent(query: $query, facets: $facets) {
      ...TorrentContentResult
    }
  }
}
